<!doctype html>
<html>
<head>
    <title>Socket.IO chat</title>
</head>
<body>
<strong>username</strong>: <%= user.username %><br>
<ul id="messages"></ul>
<form action="submit">

    <img id="img" src="./img/d1.gif"  style="width:30px;height:30px">

    <input id="m" autocomplete="off" /><button>Send</button>

</form>
<script src="https://cdn.socket.io/socket.io-1.2.0.js"></script>
<script src="http://code.jquery.com/jquery-1.11.1.js"></script>

<script type="text/javascript" language="javascript">

    var socket = io();
    $('form').submit(function () {
        var ms = Math.round(Math.random() * 5);

        socket.emit('chat message', { name1: ms });
        //$('#m').val('');
        return false;
    });
    socket.on('chat message', function(msg){
        $('#messages').append($('<li>').text(msg.name2 + msg.name1));
        var randomdice=Math.round(Math.random()*5)
        document.images["mydice"].src=eval("face"+msg.name1+".src")
        document.getElementById("player1").style.top = 200 + "px"

    });
</script>
</body>
</html>
